/**
 * DTO Interface for login form
 * @interface LoginDto
 * @property {string} email - Email
 * @property {string} password - Password
 */
export interface LoginDto {
  email: string;
  password: string;
}
